# Target-Group-Voter-Filter-
This script loads a large voter file, a list of target names (first and last), and an optional precinct‑to‑district mapping. It identifies voters whose first or last name matches any name in the target lists. The resulting filtered data (with selected columns) is saved as a CSV.
